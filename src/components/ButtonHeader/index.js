import React from "react";
import * as C from "./styles";
const ButtonHeader = ({ Text }) => {
    return (

        <C.Button>
            {Text}

        </C.Button>
    );
};
export default ButtonHeader;